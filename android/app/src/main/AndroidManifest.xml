<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- dataSync only — used by DownloadKeepAliveService while a download is
         active. Do NOT declare FOREGROUND_SERVICE_MEDIA_PLAYBACK: PiP transport
         uses Activity RemoteActions (MainActivity.mediaPendingIntent), not an
         FGS. Android 15 forbids starting dataSync/mediaPlayback from
         BOOT_COMPLETED; we never start this FGS from the boot receiver. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- EPG reminders: flutter_local_notifications reschedules AlarmManager
         alarms after reboot. The BootReceiver must NOT start any FGS. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- REQUEST_INSTALL_PACKAGES is added only by the sideload product flavor. -->

    <!-- Same APK installs on phone + Android TV. -->
    <!-- Stable ↔ Dev sources mirror: do NOT declare a shared custom permission
         here. Both packages used to define com.javp.permission.SHARED_SOURCES,
         which makes the second install fail with INSTALL_FAILED_DUPLICATE_PERMISSION
         (Fire TV only shows "OK"). SharedSourcesProvider enforces same-signer. -->

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:label="JAVP"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"/>
                <data android:scheme="https"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:scheme="magnet"/>
                <data android:mimeType="video/*"/>
                <data android:mimeType="audio/*"/>
                <data android:mimeType="application/vnd.apple.mpegurl"/>
                <data android:mimeType="application/x-mpegURL"/>
                <data android:mimeType="application/x-bittorrent"/>
            </intent-filter>
            <!-- Plex OAuth return: javp://plex-auth -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="javp" android:host="plex-auth"/>
            </intent-filter>
            <!-- LAN device pairing: javp://pair?h=&p=&t= -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="javp" android:host="pair"/>
            </intent-filter>
            <!-- One-click add source: javp://add?type=custom|m3u&url=… -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="javp" android:host="add"/>
            </intent-filter>
            <!-- Verified App Link: https://javp.app/add?… (Digital Asset Links) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="javp.app" android:pathPrefix="/add"/>
                <data android:scheme="https" android:host="javp.app" android:pathPrefix="/pair"/>
            </intent-filter>
        </activity>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <!-- Reschedules pending local notifications only (AlarmManager).
             Must never call startForegroundService — Android 15 throws
             ForegroundServiceStartNotAllowedException for dataSync /
             mediaPlayback when started from BOOT_COMPLETED. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.javp.javp.CastOptionsProvider" />
        <provider
            android:name=".SharedSourcesProvider"
            android:authorities="${applicationId}.shared_sources"
            android:exported="true" />
        <!-- Keeps HTTP/HLS/torrent downloads running when the UI is backgrounded.
             Started only from Dart (AndroidDownloadKeepAlive) while a download
             is active — never from BOOT_COMPLETED (Android 15 restriction). -->
        <service
            android:name=".DownloadKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="true"
            android:directBootAware="false" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher to open https pages (API 30+). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="http"/>
        </intent>
        <!-- Package visibility for preferring VLC when opening streams externally. -->
        <package android:name="org.videolan.vlc"/>
        <package android:name="org.videolan.vlc.debug"/>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="video/*"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="application/x-mpegURL"/>
        </intent>
    </queries>
</manifest>
