# Unused in the JAVP app binary

The Flutter app depends on `packages/rqbit_engine` (librqbit, Apache-2.0),
not this plugin. This tree is a historical GPL-3.0 fork kept only as
reference. Do not add it back to `pubspec.yaml`.

Previous notes (proxy addon, prebuilts) applied when this package was linked.
